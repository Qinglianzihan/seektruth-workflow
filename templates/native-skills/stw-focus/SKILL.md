---
name: stw-focus
description: Use when STW is in Phase 2, after investigation, to narrow work to one main contradiction, define attack zones, and prevent scope drift
---

# STW Phase 2: Focus

Thin trigger only. The current workflow rules live in `.stw/STW-Workspace.md` and CLI gates.

## Use

1. Read the current analysis and workspace files.
2. State one core task and declare the smallest required `ATTACK_ZONE` entries in `.stw/STW-Workspace.md`.
3. Run `stw next` (or `rtk stw next` if available).
4. If blocked, narrow scope or fix the declared zones only.

Do not code before Phase 3.
